import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("The app is running 🎉.")
            .padding()
            .font(.title)
            .multilineTextAlignment(.center)
    }
}
